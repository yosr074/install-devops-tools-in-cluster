apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: kubernetes-dashboard-domain
  namespace: kube-system
spec:
  rules:
  - host: dashboard.wl.consulting
    http:
      paths:
      - path: /
        backend:
          serviceName: kubernetes-dashboard
          servicePort: 80

