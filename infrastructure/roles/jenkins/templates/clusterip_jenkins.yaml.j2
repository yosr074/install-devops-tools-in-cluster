apiVersion: v1
kind: Service
metadata:
  name: {{ jenkins_service_clusterip_name }}
  labels:
    app: jenkins-service
  namespace: {{ namespace }}
spec:
  ports:
  - port: {{ jenkins_port }}
    protocol: TCP
  selector:
    app: jenkins
 
